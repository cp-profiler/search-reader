This utility reads the output produced by complimentary search-recorder utility and redirects it to CP-Profiler.

Debug mode is available (`-d`, `--debug`) that lets the user send messages one at a time.